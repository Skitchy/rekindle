## Session End Routine

1. Store a checkpoint: summarize what was accomplished, current state, and next steps
   Use `store_memory` with category "context", importance 7, and your project name
2. If anything identity-relevant changed this session, update .rekindle/identity.md
3. The session transcript will be captured automatically by the hook (if configured)
